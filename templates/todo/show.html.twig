{% extends "base.html.twig" %}

{% block body %}
    <div class="container my-5">
        <div class="row">
            <h1>{{ title }}</h1>
        </div>
        <div class="row">
        <p>{{ todo.getStatus() }}</p>
        <p>{{ todo.getPriority() }}</p>
        <p>{{ todo.getDateCreation() | date("m/d/Y") }}</p>
            
        </div>
    </div>
{% endblock%}
